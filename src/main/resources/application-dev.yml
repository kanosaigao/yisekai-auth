spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://xxx:3306/kanosaigao?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root

  redis:
    host: xxx
    port: 6379
    password: xxx
    database: 1


# token配置
token:
  # 令牌自定义标识
  header: Authorization
  # 令牌密钥
  secret: kanosaigao
  # 令牌有效期（单位分钟）
  expireTime: 1440